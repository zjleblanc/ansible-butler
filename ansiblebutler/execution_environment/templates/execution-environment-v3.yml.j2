# Generated by ansible-butler
# {{ date.strftime('%m-%d-%y %H:%M') }}
---
version: {{ version }}
{% if build_arg_defaults is defined %}

build_arg_defaults:
{{ build_arg_defaults | to_nice_yaml(level=1) }}
{% endif %}

images:
  base_image:
    name: {{ ee_base_image }}

# Example usage of dependencies
dependencies: 
{{ dependencies | to_nice_yaml(level=1) }}

additional_build_steps: 
{{ additional_build_steps | to_nice_yaml(level=1) }}